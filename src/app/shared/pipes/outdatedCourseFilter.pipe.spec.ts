import { OutdatedCourseFilterPipe } from './outdatedCourseFilter.pipe';

describe(`DurationPipe`, () => {
    let pipe: OutdatedCourseFilterPipe;

    beforeEach(() => {
        pipe = new OutdatedCourseFilterPipe();
    });

    // it('transforms "0" to "0 min"', () => {
    //     expect(pipe.transform(0)).toBe('0 min');
    // });

});
